# Repository collaboration instructions

- 每次完成代码或脚本修改后，最终结果必须包含“新的执行命令”，给出可直接复制的工作目录和完整命令。
- 如果同时存在预览、测试和正式执行命令，必须分别标注其用途，并明确哪一条会产生真实外部操作。
- 如果代码修改后无需执行任何命令，也要明确写出“无需额外执行命令”。
- 每次完成代码、配置或脚本修改并验证后，自动仅 `git add` 本次相关文件并创建 git commit；不得带入无关改动，不自动 push。最终报告提交号，使用户只需执行 `git push`。
- 项目程序在服务器上执行。最终命令不得包含 `/Users/...` 等本地绝对路径；默认以服务器仓库根目录为起点，使用 `cd intent_behavior && ...` 形式给出可复制命令。
